import { computed, ref } from 'vue'
import { defineStore } from 'pinia'
import { backend } from '../services/api'
type Session = { username: string; roles: string[]; expiresAt: number }
const saved = () => { try { return JSON.parse(sessionStorage.getItem('ragkb-session') || 'null') as Session | null } catch { return null } }
function tokenPayload(token: string): { roles?: string[]; exp?: number } { try { const encoded = token.split('.')[1]; if (!encoded) return {}; return JSON.parse(atob(encoded.replace(/-/g, '+').replace(/_/g, '/'))) } catch { return {} } }
export const useAuthStore = defineStore('auth', () => { const session = ref<Session | null>(saved()); const isAuthenticated = computed(() => !!session.value && session.value.expiresAt > Date.now()); const username = computed(() => session.value?.username || ''); const roles = computed(() => session.value?.roles || []); const initials = computed(() => username.value.split(/[._\s]/).map(x => x[0]).join('').slice(0,2).toUpperCase() || 'U'); async function login(user:string,password:string) { const { data } = await backend.login(user, password); const response=data as typeof data & { accessToken?: string; expiresIn?: number }; const token=response.token || response.accessToken; if (!token) throw new Error('O backend não retornou o token de acesso.'); const payload=tokenPayload(token); const newSession={username:user,roles:Array.isArray(payload.roles)?payload.roles:[],expiresAt:payload.exp ? payload.exp*1000 : Date.now()+(response.expiresIn || 3600)*1000}; session.value=newSession;sessionStorage.setItem('ragkb-session',JSON.stringify(newSession));sessionStorage.setItem('ragkb-token',token) }; function logout(){session.value=null;sessionStorage.removeItem('ragkb-session');sessionStorage.removeItem('ragkb-token')}; function hasRole(role:string){return roles.value.includes(role)};function hasAnyRole(check:string[]){return check.some(hasRole)};return{isAuthenticated,username,roles,initials,login,logout,hasRole,hasAnyRole} })
